"use strict";
const r = {
    numDoors: 1,
    ceilingHeightft: 10,
    // elephant: 'present',
};
function createWindow(options) {
    if (options.darkMode) {
        // setDarkMode();
    }
}
createWindow({
    title: 'Spider Solitaire',
    darkMode: true,
});
const o1 = document;
const o2 = new HTMLAnchorElement();
